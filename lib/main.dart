import 'package:flutter/material.dart'; // Provides essential Flutter widgets for building UI components
import 'app.dart'; // Imports the main application file containing the NomNomApp class

/// The main function serves as the entry point for the application.
/// It initializes the Flutter framework and starts the app by running the `NomNomApp` widget.
void main() {
  runApp(const NomNomApp()); // Launch the NomNom application.
}
